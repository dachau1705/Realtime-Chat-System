// Local Developer IP address configuration for physical device / emulator testing
export const LOCAL_IP = '10.0.2.2';

export const API_URL = `http://${LOCAL_IP}:3000`;
export const WS_URL = `http://${LOCAL_IP}:3001`;
